version: '3.8'

services:
  app:
    # Build the Docker image from the Dockerfile in the current directory
    build: .
    # Name the container for easier identification
    container_name: my-app
    # Restart policy to ensure the container comes back up if it stops
    restart: unless-stopped

    # The following sections are placeholders. Uncomment and modify as needed.

    # Port mapping: <HOST_PORT>:<CONTAINER_PORT>
    # ports:
    #   - "8080:8080"

    # Volume mounting for development (syncs local code with the container)
    # volumes:
    #   - .:/app

    # Environment variables
    # environment:
    #   - DB_HOST=database
    #   - NODE_ENV=development
